stages:
  - build
  - deploy

variables:
  DOCKER_DRIVER: overlay2

build_image:
  stage: build
  script:
    - docker build -t yourdockerhubusername/devops-node-app:v1 .

deploy_app:
  stage: deploy
  script:
    - docker-compose down
    - docker-compose up -d